{
  "buildCommand": "npm ci --prefer-offline --no-audit",
  "installCommand": "npm ci --prefer-offline --no-audit",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
}
